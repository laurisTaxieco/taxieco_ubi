// Cookie Names

export const idTokenCookie = "identity";

export const accessTokenCookie = "access";

export const refreshTokenCookie = "refresh";

// URLs -  Auth

export const loginUrl = "/api/auth/login";

export const refreshUrl = "/api/auth/refresh";

// URLs - QuickSight

export const getDashboardUrl = "/api/get-dashboard";
